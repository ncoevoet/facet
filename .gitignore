# Python
__pycache__/
*.py[cod]
*$py.class
venv/
.venv/
env/

# Test coverage (pytest-cov)
.coverage
.coverage.*
htmlcov/
coverage.xml

# IDE
.idea/
.vscode/
*.swp
*.swo

# Project specific
debug.log
*.db
*.db.backup.*
*.db-shm
*.db-wal
*.db-journal
pretrained_models/
aesthetic_predictor_weights.pth

# Test/temp data
photos/
@eaDir/

# Config backups — both the bare name the secret migration and the password
# upgrade write, and the timestamped ones from the config writers. The bare
# name is NOT covered by the `.backup.*` pattern: it has no suffix.
scoring_config.json.backup
scoring_config.json.backup.*

# Server secret — signs every session JWT and frame link. Never tracked: it
# used to live in scoring_config.json and got committed from there.
.facet_secret

# Claude Code
.claude/todos.md
.claude/snapshot*.txt
.claude/review-all/
.codegraph/

# Build output
dist/

# Node — exact versions pinned in client/package.json, lock file not tracked
client/node_modules/
client/package-lock.json

# ML model cache
.fastembed_cache/
model-cache/

# Misc
output.log
*.db.shrink
sample.*
test-screenshots/
test_*.png
20??-*.png
.git_diff_tmp
.mcp.json
plugins/example_plugin.py.example
facet_photo.egg-info/
photos_all/

# Archives
*.zip

# Benchmark results — keep dir via .gitkeep, ignore generated JSON
bench/results/*.json
.claude/loop/

