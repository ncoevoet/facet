# Facet Dependencies
# Install with: pip install -r requirements.txt
# Note: Also requires 'exiftool' command-line tool (install via system package manager)

# Deep Learning / GPU
torch>=2.0.0
torchvision>=0.15.0
open-clip-torch>=2.20.0

# Advanced VLM Models - only needed for 16gb/24gb VRAM profiles
# Uncomment if using "vram_profile": "16gb" or "24gb":
# transformers>=5.0.0              # Required for Qwen3-VL / Qwen2.5-VL taggers
# accelerate>=0.25.0

# Image Processing
opencv-python>=4.8.0
pillow>=10.0.0
imagehash>=4.3.0
rawpy>=0.18.0

# Face Detection
insightface>=0.7.0
# ONNX Runtime - choose ONE based on your setup:
# onnxruntime>=1.15.0           # CPU only (works everywhere)
# onnxruntime-gpu>=1.17.0       # GPU acceleration for CUDA 12.x
# onnxruntime-gpu>=1.15.0,<1.18 # GPU acceleration for CUDA 11.8

# Quality Assessment (PyIQA models: TOPIQ, HyperIQA, DBCNN, MUSIQ)
pyiqa>=0.1.10

# Scientific Computing
numpy>=1.24.0
scipy>=1.10.0
hdbscan>=0.8.40  # Face clustering (standalone library, sklearn's HDBSCAN has epsilon bug)

# Web Interface
fastapi>=0.100.0
uvicorn>=0.23.0
pyjwt>=2.8.0

# Utilities
tqdm>=4.65.0
exifread>=3.0.0

# System Monitoring (for batch processing auto-tuning)
psutil>=5.9.0

# Subject Saliency (optional, requires GPU)
# transparent-background>=2.0.0  # InSPyReNet saliency model

# GPU Face Clustering (optional, requires conda + CUDA)
# cuml
# cupy
